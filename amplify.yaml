version: 1
frontend:
  phases:
    preBuild:
      - echo "built"
    build:
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: book/book
    files:
      - '**/*'
