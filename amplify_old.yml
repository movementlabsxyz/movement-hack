version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd book && mdbook build
    build:
      commands:
        - cp -r book/book public
  artifacts:
    baseDirectory: public
    files:
      - '**/*'

